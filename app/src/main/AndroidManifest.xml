<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.emma.uniwork" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <meta-data
            android:name="com.google.android.maps.MapKey"
            android:value= "AIzaSyCrcyLTRHi5M12WIoDZOGnXcpxDYeKfUN0" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".mcOutputScreen"
            android:label="text"></activity>

        <activity android:name=".mcBioActivity"
        android:label = "text"></activity>

        <activity android:name=".mcSavingDataOutput"
            android:label="@string/mc_SavedData"></activity>

        <activity android:name=".mcMapActivity"
            android:label="@string/mc_Map"/>

        <activity android:name=".PoemOutput"
        android:label="mcPoemOutput"/>

        <activity android:name=".mcShakeAccCrystalBall"
        android:label="mcCrystalBall"/>
        
    </application>

</manifest>
